---
phase: 02-pgvector-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/config/config.go
  - backend/internal/config/config_test.go
  - backend/.env.example
  - backend/internal/ai/embed_spike_test.go
autonomous: true
requirements: [PGV-04a]
must_haves:
  truths:
    - "The canonical embedding model (text-embedding-3-small, 1536-dim) is locked in config BEFORE any ai_vectors DDL"
    - "A Nigerian-language embedding adequacy spike has confirmed or surfaced a blocker on the canonical model"
    - "Startup fails fast if AI_EMBEDDING_DIM is missing or exceeds the HNSW 2000-dim cap"
  artifacts:
    - path: "backend/internal/config/config.go"
      provides: "AI_EMBEDDING_DIM config field + fail-fast startup validation"
      contains: "EmbeddingDim"
    - path: "backend/.env.example"
      provides: "Documented AI_EMBEDDING_DIM=1536"
      contains: "AI_EMBEDDING_DIM"
    - path: "backend/internal/ai/embed_spike_test.go"
      provides: "Nigerian-language embedding adequacy eval on the canonical model"
      contains: "text-embedding-3-small"
  key_links:
    - from: "backend/internal/config/config.go"
      to: "validate()"
      via: "error when EmbeddingDim <= 0 or > 2000"
    - from: "backend/internal/ai/embed_spike_test.go"
      to: "text-embedding-3-small"
      via: "asserts output dimension == 1536 and paraphrase similarity thresholds"
---

<objective>
Lock the canonical embedding model and dimension (D-01: `text-embedding-3-small`, 1536-dim, `AI_EMBEDDING_DIM=1536`) and prove it adequate for Nigerian-language multilingual content BEFORE any `ai_vectors` DDL is written.

Purpose: PGV-04a is a HARD Phase-2 entry blocker — the table's `vector(1536)` column and HNSW index are frozen once DDL ships; a wrong canon means a migration and full re-embed of all tenant data. This plan pins the canon and installs the fail-fast dimension guard (D-14).
Output: `AI_EMBEDDING_DIM` config + validation, a Nigerian-language eval spike test, and `02-01-SPIKE.md` recording the result.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pgvector-migration/02-CONTEXT.md (D-01, D-02, D-03, D-14)
@.planning/phases/02-pgvector-migration/02-RESEARCH.md (canon lock, dimension cap)

<interfaces>
From backend/internal/ai/openai.go:
- Model constant already in use: `openai.EmbeddingModelTextEmbedding3Small` == "text-embedding-3-small" (1536-dim, D-01).
From backend/internal/ai/gateway.go (the real provider contract — NOT provider.go; provider.go does not exist):
- `ai.NewProvider(cfg AIServiceConfig) (Provider, error)` (gateway.go:116); `Provider.GenerateEmbeddings(ctx, texts []string) ([]*EmbeddingResponse, error)` (gateway.go:71) where `EmbeddingResponse{Embedding []float32, Model string, Tokens int}` (gateway.go:49).
From backend/internal/ai/config.go:
- `AIServiceConfig` struct (Enabled/Provider/OpenAIAPIKey/OpenAIModel/MaxTokens/...) — the `cfg` argument passed to `NewProvider`; built from app config via `FromAppConfig(cfg config.AIConfig)`.
From backend/internal/config/config.go:
- `AIConfig` struct (lines ~96-113) with `Provider`, `OpenAIAPIKey`, `OpenAIModel`, `QdrantURL env:"AI_QDRANT_URL"`, `EngineURL`, `EngineToken`.
- `validate()` (line ~408) already fails fast on `JWT_SECRET`, `ENCRYPTION_KEY`, `AI_ENGINE_URL` (must be http(s)), `AI_ENGINE_TOKEN`. New `AI_EMBEDDING_DIM` validation joins here.
- Defaults pattern: `QdrantTimeout getInt("AI_QDRANT_TIMEOUT", 30)` at line ~316.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Nigerian-language embedding adequacy spike</name>
  <files>backend/internal/ai/embed_spike_test.go (new), 02-01-SPIKE.md (report artifact in this phase dir)</files>
  <read_first>backend/internal/ai/openai.go (embedding path + model constant), backend/internal/ai/gateway.go (NewProvider + Provider interface + EmbeddingResponse), backend/internal/ai/config.go (AIServiceConfig struct), backend/.env (check for AI_OPENAI_API_KEY / AI_PROVIDER / AI_OPENAI_MODEL)</read_first>
  <behavior>
    - Embed canonical test sentence + its near-paraphrase (Nigerian Pidgin + Hausa pairs) via the SAME ModelRouter path the app uses (ai.NewProvider with Provider=openai, OpenAIModel=text-embedding-3-small).
    - Assert output dimension == 1536 for every embedded text (canon lock, D-01/D-14). If the router returns 3072 (Gemini misconfig), the test FAILS → executor must fix .env provider config to the canonical model before proceeding.
    - Assert cosine similarity of paraphrase pairs > 0.75; unrelated pairs < 0.60.
    - If no AI_OPENAI_API_KEY is present in env, t.Skip with a documented rationale — the locked decision (D-01) still stands; record the skip in 02-01-SPIKE.md.
  </behavior>
  <action>Create `backend/internal/ai/embed_spike_test.go` in package `ai` with `func TestEmbeddingNigerianLanguageAdequacy(t *testing.T)`. Build the provider from env (skip when no key). Corpus (Nigerian Pidgin/Hausa): at least 3 paraphrase pairs (e.g. "I wan learn how to read dis book" / "I dey try to learn reading this book", Hausa "Ina son karanta littafin" / "Ina so in karanta littafi") and 2 unrelated pairs (e.g. "The weather is hot today" / "I need to pay school fees"). Use cosine similarity on the 1536-dim vectors. Thresholds: paraphrase &gt; 0.75, unrelated &lt; 0.60. Log per-pair scores with logger/t.Log and assert dim == 1536 on every result. After the run, write `02-01-SPIKE.md` in `.planning/phases/02-pgvector-migration/`: model, dimension, per-pair scores, pass/fail, and (if skipped) the skip rationale + statement that D-01 canon stands pending future eval. If the eval FAILS quality thresholds, do NOT proceed to any ai_vectors DDL — record the gap in the SPIKE report and surface a blocker to the user (PGV-04a gate).</action>
  <verify>
    <automated>cd backend && go test ./internal/ai/ -run TestEmbeddingNigerianLanguageAdequacy -v</automated>
  </verify>
  <done>Spike test runs (passes with key, or cleanly skips without); 02-01-SPIKE.md records result; dimension assertion == 1536 enforced.</done>
</task>

<task type="auto">
  <name>Task 2: Lock AI_EMBEDDING_DIM config with fail-fast validation</name>
  <files>backend/internal/config/config.go, backend/internal/config/config_test.go, backend/.env.example</files>
  <read_first>backend/internal/config/config.go (AIConfig struct lines ~96-113, defaults ~316, validate() ~408), backend/internal/config/config_test.go (valid fixture lines ~141-142, negative tests pattern lines ~150-186)</read_first>
  <action>Per D-01/D-14: add `EmbeddingDim int` to `AIConfig` with env tag `AI_EMBEDDING_DIM`, parsed via the existing `getInt` helper, default 1536 (D-01 locked). In `validate()`: if `EmbeddingDim <= 0` → `fmt.Errorf("AI_EMBEDDING_DIM is required")`; if `EmbeddingDim > 2000` → `fmt.Errorf("AI_EMBEDDING_DIM %d exceeds HNSW vector dimension cap of 2000", ...)` (HNSW cap per RESEARCH; Rule B12 fail-fast, no silent default). Update `config_test.go`: add `EmbeddingDim: 1536` to the valid fixture; add two negative tests following the existing pattern — `t.Run("missing AI_EMBEDDING_DIM rejected")` (fixture without EmbeddingDim → `assert.ErrorContains(t, err, "AI_EMBEDDING_DIM")`) and `t.Run("oversized AI_EMBEDDING_DIM rejected")` (EmbeddingDim 3072 → error contains "2000"). Add `AI_EMBEDDING_DIM=1536` to `backend/.env.example` next to the AI_ENGINE_* lines. Also set `AI_EMBEDDING_DIM=1536` in `backend/.env` (executor; gitignored — do not commit) so the server starts. Commit only .env.example, not .env.</action>
  <verify>
    <automated>cd backend && go test ./internal/config/ -v</automated>
  </verify>
  <done>validate() rejects missing/oversized dimension; valid fixture passes; .env.example documents AI_EMBEDDING_DIM=1536.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file → validate() | Untrusted/omitted env values cross into startup |
| embedder output → vector column | Model output dimension vs declared vector(n) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PGV-01-01 | D | config.go validate() | mitigate | AI_EMBEDDING_DIM required (>0) and ≤2000; error, not default (Rule B12) |
| T-PGV-01-02 | I | embed_spike_test.go | mitigate | Assert output dim == 1536 on every embed; misconfigured provider fails the test |
| T-PGV-01-03 | D | eval gate | accept | Spike may skip when no API key — canon already locked (D-01); skip rationale recorded in 02-01-SPIKE.md |
| T-PGV-01-04 | I | config.go validate() | mitigate | No fallback default for missing dimension (D-14); regression test covers missing + oversized cases |
</threat_model>

<verification>
- `cd backend && go test ./internal/config/ -v` — all pass.
- `cd backend && go test ./internal/ai/ -run TestEmbeddingNigerianLanguageAdequacy -v` — passes or skips cleanly.
- `02-01-SPIKE.md` exists with model, dimension, scores, and result.
</verification>

<success_criteria>
- Canon locked before any DDL: `AI_EMBEDDING_DIM=1536`, model `text-embedding-3-small` — both recorded in SPIKE report and config.
- Startup fails fast on missing (>0) or oversized (>2000) dimension.
- No `ai_vectors` DDL written anywhere yet (PGV-04a gate respected).
</success_criteria>

<output>
After completion, create `.planning/phases/02-pgvector-migration/02-01-SUMMARY.md`
</output>
