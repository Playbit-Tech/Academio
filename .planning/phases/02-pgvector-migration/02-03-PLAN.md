---
phase: 02-pgvector-migration
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - backend/internal/database/migrations/school/school.go
autonomous: true
requirements: [PGV-04]
must_haves:
  truths:
    - "Every school_{id} TENANT schema has an ai_vectors table (not public)"
    - "The embedding column is public.vector(1536) matching AI_EMBEDDING_DIM"
    - "A per-schema HNSW index (vector_cosine_ops) exists with raised maintenance_work_mem"
    - "A unique constraint on (document_id, chunk_index) exists"
  artifacts:
    - path: "backend/internal/database/migrations/school/school.go"
      provides: "Tenant ai_vectors DDL migration with schema-qualified types and HNSW index"
      contains: "2026_08_01_000001_create_ai_vectors"
  key_links:
    - from: "backend/internal/database/migrations/school/school.go"
      to: "school_{id} schemas"
      via: "school migration runner (SET LOCAL search_path TO school_N — public NOT on path)"
    - from: "embedding public.vector(1536)"
      to: "AI_EMBEDDING_DIM"
      via: "1536 constant locked in plan 02-01 (D-01/D-14)"
---

<objective>
Create the `ai_vectors` table in each tenant schema with a 1536-dim HNSW index — structurally tenant-scoped, no partial-index machinery (PGV-04).

Purpose: This is the storage substrate the `vector.Store` implementation (plan 04) and the copy tool (plan 05) write to. It MUST be schema-per-tenant (not shared + partial indexes) per ROADMAP and ARCHITECTURE. The DDL runs after the embedding canon is locked (02-01) and the extension exists (02-02).
Output: `ai_vectors` + `idx_ai_vectors_embedding_hnsw` (+ collection index + unique constraint) in every provisioned tenant schema.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pgvector-migration/02-RESEARCH.md (schema-qualified DDL requirement, maintenance_work_mem, migration IDs)
@.planning/phases/02-pgvector-migration/02-CONTEXT.md (D-04..D-09, D-14)

<interfaces>
From backend/internal/database/migrations/school/school.go:
- `func SchoolMigrations() []migration.Migration` — pattern to mirror: `2024_01_01_000000_enable_pgcrypto_extension` (`CREATE EXTENSION IF NOT EXISTS "pgcrypto"`).
From backend/internal/database/tenant/migration_service.go:
- `ApplySchoolMigrationsForSchema` (line ~99) and `CreateSchemaAndMigrate` (line ~159) run each school migration with `SET LOCAL search_path TO school_N` — so UNQUALIFIED table names resolve to the tenant schema, and `public` is NOT on the search_path. Therefore every type/opclass reference MUST be schema-qualified: `public.vector`, `public.vector_cosine_ops`.
From backend/internal/database/migration/types.go:
- `Migration{ ID string; Migrate func(db *gorm.DB) error; Rollback func(db *gorm.DB) error }` — fields are `Migrate`/`Rollback`.
From backend/cmd/migrate-schemas/main.go:
- Applies pending school migrations to every provisioned schema; supports `--dry-run` and a concurrency flag. Use this to apply the new DDL to existing schemas.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the tenant ai_vectors DDL migration (PGV-04)</name>
  <files>backend/internal/database/migrations/school/school.go</files>
  <read_first>backend/internal/database/migrations/school/school.go (pgcrypto migration pattern), backend/internal/database/tenant/migration_service.go lines 90-170 (search_path mechanics), backend/internal/database/migration/types.go</read_first>
  <action>Append to the `SchoolMigrations()` slice a migration with `ID: "2026_08_01_000001_create_ai_vectors"` whose `Migrate` runs these separate `db.Exec` calls IN THIS ORDER (one statement per Exec — Rule B4; types schema-qualified — `public.vector(1536)` and `public.vector_cosine_ops`, because the runner's search_path is schema-only and `public` is not on it):
  1. `CREATE TABLE IF NOT EXISTS ai_vectors (id VARCHAR(255) PRIMARY KEY, collection VARCHAR(255) NOT NULL, embedding public.vector(1536) NOT NULL, document_id VARCHAR(255) NOT NULL, chunk_index VARCHAR(255) NOT NULL, text TEXT NOT NULL, embedding_model VARCHAR(255) NOT NULL, model_version VARCHAR(255) NOT NULL, chunking_version VARCHAR(255) NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now(), CONSTRAINT uq_ai_vectors_doc_chunk UNIQUE (document_id, chunk_index))` — column set per ROADMAP success criterion 3 (metadata columns `embedding_model`, `model_version`, `chunking_version`; unique constraint on `(document_id, chunk_index)`). **D-09 deviation annotation (MANDATORY):** D-09 lists `metadata jsonb` + timestamps. This DDL intentionally OMITS jsonb — metadata is FLATTENED into the three parity columns `document_id`, `chunk_index`, `text`, which map 1:1 to the Qdrant payload keys `_doc_id`, `_chunk_index`, `_text` (the real pipeline contract at `internal/ai/rag/pipeline.go:116-118`; also the mapping the copy tool in plan 02-05 uses). jsonb is deliberately omitted per the agent's Discretion (D-09 explicitly allows columns-vs-jsonb; flattened columns prevent payload-key drift between the store and the copy tool). Both `created_at` AND `updated_at` ARE included per D-09's "timestamps" (matching the research DDL template) — `updated_at` defaults to now() and is present for write-path updates even though the v1 store only inserts. Prepend a SQL comment as the FIRST line of this Exec so the contract is self-documenting in the database — `-- ai_vectors preserves Qdrant payload parity keys (_doc_id, _chunk_index, _text) as document_id/chunk_index/text; metadata flattened, jsonb intentionally omitted (D-09 agent discretion); timestamps created_at/updated_at included` — comment + CREATE TABLE is still ONE statement in one Exec (Rule B4). Table name UNQUALIFIED — resolves to `school_N` via search_path. 1536 MUST equal `AI_EMBEDDING_DIM` (locked in 02-01; D-14).
  2. `CREATE INDEX IF NOT EXISTS idx_ai_vectors_collection ON ai_vectors (collection)`
  3. `SET LOCAL maintenance_work_mem = '128MB'` (raised per ROADMAP — HNSW builds are memory-hungry; SET LOCAL scopes to the transaction)
  4. `CREATE INDEX IF NOT EXISTS idx_ai_vectors_embedding_hnsw ON ai_vectors USING hnsw (embedding public.vector_cosine_ops) WITH (m = 16, ef_construction = 64)` — the `<=>` operator maps to `vector_cosine_ops`; HNSW supports ≤2000-dim vectors (1536 ok).
  `Rollback`: `DROP TABLE IF EXISTS ai_vectors` (drops indexes with it). Then `cd backend && go build ./...`.</action>
  <verify>
    <automated>cd backend && go build ./...</automated>
  </verify>
  <done>Migration appended with the exact 4-step DDL (each its own Exec), schema-qualified types, unique constraint, HNSW index with raised maintenance_work_mem.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the migration to every existing tenant schema (PGV-04)</name>
  <files>backend/cmd/migrate-schemas/main.go (run only — no modification)</files>
  <read_first>backend/cmd/migrate-schemas/main.go (flags: --dry-run, concurrency)</read_first>
  <action>Do NOT recreate the database. Apply pending school migrations to all provisioned schemas: `cd backend && go run ./cmd/migrate-schemas --dry-run` first (review the plan), then run without `--dry-run`. This routes through `CreateSchemaAndMigrate`/`MigrateAllSchemaTenants` so every `school_{id}` schema gets the new DDL. Restart the backend server if not already running. Then verify EVERY tenant schema (loop `SELECT schemaname FROM pg_tables WHERE tablename='ai_vectors'`): table exists, both indexes exist, and `format_type` of the embedding column is `public.vector(1536)`.</action>
  <verify>
    <automated>docker exec shared-postgres psql -U postgres -d academio -tAc "SELECT schemaname FROM pg_tables WHERE tablename='ai_vectors' ORDER BY 1" && docker exec shared-postgres psql -U postgres -d academio -tAc "SELECT indexname FROM pg_indexes WHERE tablename='ai_vectors' AND indexname LIKE 'idx_ai_vectors%' ORDER BY 1" && docker exec shared-postgres psql -U postgres -d academio -tAc "SELECT format_type(atttypid, atttypmod) FROM pg_attribute WHERE attrelid='school_1.ai_vectors'::regclass AND attname='embedding'"</automated>
  </verify>
  <done>Every school_N schema has ai_vectors with `idx_ai_vectors_collection` + `idx_ai_vectors_embedding_hnsw` (hnsw method), embedding column typed `public.vector(1536)`, unique constraint `uq_ai_vectors_doc_chunk` present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration DDL → tenant schema | Untrusted DDL content executes inside each school_{id} schema |
| extension type resolution → search_path | Type/opclass lookups rely on schema qualification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PGV-04-01 | D | school.go DDL | mitigate | Type names schema-qualified (`public.vector`, `public.vector_cosine_ops`) because tenant search_path excludes public — unqualified resolution would fail the migration loudly |
| T-PGV-04-02 | D | HNSW index | mitigate | Verify `pg_indexes` shows `hnsw` index; queries ORDER BY `embedding <=> ?` (vector_cosine_ops) so the index is usable; plan 04 keeps the raw operator in ORDER BY |
| T-PGV-04-03 | I | vector(1536) vs config | mitigate | 1536 constant locked to AI_EMBEDDING_DIM in 02-01; startup dimension probe in 06 fail-fasts on mismatch (D-14) |
| T-PGV-04-04 | D | cross-schema bleed | mitigate | DDL is schema-local (runs inside each tenant schema); extension owned by public only, not copied per tenant |
</threat_model>

<verification>
- `go build ./...` passes.
- Per tenant schema: table exists, `idx_ai_vectors_embedding_hnsw` uses `hnsw`, `format_type` == `public.vector(1536)`, unique constraint present.
</verification>

<success_criteria>
- `ai_vectors` exists in EVERY `school_{id}` tenant schema (never `public`).
- HNSW index + collection index + unique(doc, chunk) constraint per schema.
- Column set matches ROADMAP criterion 3 (embedding_model, model_version, chunking_version).
</success_criteria>

<output>
After completion, create `.planning/phases/02-pgvector-migration/02-03-SUMMARY.md`
</output>
