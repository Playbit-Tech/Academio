---
phase: 02-pgvector-migration
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-03]
files_modified:
  - backend/go.mod
  - backend/go.sum
  - backend/internal/ai/vector/pgvector.go
  - backend/internal/ai/vector/pgvector_test.go
autonomous: true
requirements: [PGV-03]
must_haves:
  truths:
    - "The Go RAG pipeline and all agents work unchanged against pgvector (zero RAG/agent changes)"
    - "Insert/Search/Delete resolve tenancy from context and always hit the school_{id} schema"
    - "Search results carry _text (and _doc_id/_chunk_index) metadata for the agent contract"
    - "Scores are cosine similarity (score = 1 - distance), matching Qdrant semantics"
    - "Embedding dimension mismatches fail loudly, never silently corrupt"
  artifacts:
    - path: "backend/internal/ai/vector/pgvector.go"
      provides: "PGVectorStore implementing the vector.Store interface"
      contains: "func (s *PGVectorStore) Search"
    - path: "backend/go.mod"
      provides: "pgvector-go dependency"
      contains: "pgvector/pgvector-go"
    - path: "backend/internal/ai/vector/pgvector_test.go"
      provides: "Unit + integration conformance tests"
      contains: "TestPGVectorStoreIntegration"
  key_links:
    - from: "pgvector.go Insert"
      to: "school_{id}.ai_vectors"
      via: "repos.TenantDB().Table(\"ai_vectors\") (SchemaTablePrefix plugin)"
    - from: "pgvector.go Search"
      to: "score conversion"
      via: "SELECT 1 - (embedding <=> $1) AS score ... ORDER BY embedding <=> $1"
    - from: "SearchResult.Metadata[\"_text\"]"
      to: "backend/internal/ai/agents/academic_tutor.go"
      via: "r.Metadata[\"_text\"] (line ~192)"
---

<objective>
Implement `PGVectorStore` in `backend/internal/ai/vector/pgvector.go` implementing the existing `vector.Store` interface — insert/search/delete vectors in the per-tenant `ai_vectors` table with zero changes to the RAG pipeline or any agent (PGV-03).

Purpose: This is the drop-in replacement for the Qdrant store. It must be interface-conformant (same method set and collection-string semantics), resolve tenancy from `context.Context` (no `*gin.Context`), convert pgvector cosine distance to Qdrant-compatible similarity (`score = 1 - distance`), and enforce the dimension canon (D-14).
Output: Working `PGVectorStore` + tests (unit: schema validation, metadata mapping; integration: insert/search/delete parity against a real tenant schema).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pgvector-migration/02-RESEARCH.md (tenancy, score parity, pgvector-go v0.4.1)
@.planning/phases/02-pgvector-migration/02-CONTEXT.md (D-05, D-06, D-08, D-14)

<interfaces>
From backend/internal/ai/vector/store.go (THE contract — no changes allowed):
```go
type VectorDocument struct {
    ID        string
    Embedding []float32
    Metadata  map[string]string
}
type SearchResult struct {
    ID       string
    Score    float64
    Metadata map[string]string
}
type Store interface {
    Insert(ctx context.Context, collection string, docs []VectorDocument) error
    Search(ctx context.Context, collection string, query []float32, limit int) ([]SearchResult, error)
    Delete(ctx context.Context, collection string, ids []string) error
    Close() error
}
```
From backend/internal/database/tenant/schema_db.go:
- `SchemaTablePrefix()` plugin prefixes `db.Statement.Table` with the schema for Create/Query/Update/Delete/Row/Raw callbacks. A session obtained via `SchemaDB.DBWithContext(parentCtx)` (or `TenantRepositories.TenantDB()`, which is already schema-scoped) keeps the schema prefix through `.WithContext()`. Plugin prefixes ONLY the GORM-builder table — RAW SQL text is NOT rewritten, so raw queries must use the schema-qualified table name built from `repos.SchemaName()` (trusted DB value).
From backend/internal/middleware/tenant.go:
- `CtxKeyTenantRepos contextKey = "tenant_repos"` — set on the Go context at line ~311; `ctx.Value(middleware.CtxKeyTenantRepos)` yields `*tenant.TenantRepositories`.
From backend/internal/middleware/audit.go:
- `GetSchoolIDFromCtx(ctx context.Context) uint` (line ~240) — 0 when absent.
From backend/internal/database/tenant/factory.go:
- `(*TenantRepositories).TenantDB() *gorm.DB` and `(*TenantRepositories).SchemaName() string`.
- `(*RepositoryFactory).ForSchoolSchema(ctx, schoolID uint, schemaName string) (*TenantRepositories, error)` (line ~75).
From pgvector-go v0.4.1:
- `pgvector.NewVector([]float32) pgvector.Vector` (driver.Valuer + sql.Scanner); pass it as the query param for `<=>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pgvector-go and write failing tests (RED)</name>
  <files>backend/go.mod, backend/go.sum, backend/internal/ai/vector/pgvector_test.go (new)</files>
  <read_first>backend/internal/ai/vector/store.go (interface), backend/internal/ai/vector/qdrant.go (existing store pattern), backend/internal/database/tenant/factory.go, backend/internal/middleware/tenant.go (CtxKeyTenantRepos), backend/internal/middleware/audit.go (GetSchoolIDFromCtx)</read_first>
  <behavior>
    - Test 1 (TestValidSchemaName): "school_1" valid; "public" invalid; "school_x;DROP TABLE ai_vectors" invalid; "" invalid; "School_1" invalid.
    - Test 2 (TestVectorDocumentMapping): metadata {_doc_id, _chunk_index, _text} maps to document_id/chunk_index/text row fields; missing _chunk_index returns an error (no silent defaults).
    - Test 3 (TestPGVectorStoreIntegration): guarded by env `PGVECTOR_TEST_DSN` (or DB_* from backend/.env → shared-postgres, schema school_1 — requires plan 02-03 DDL applied). Insert two docs (different collections, dim 1536); Search returns the nearest doc first with score ≈ 1 - distance (delta ≤ 0.001); Metadata["_text"] round-trips; Delete removes and subsequent Search is empty.
    - The package must fail to compile (pgvector.go does not exist) — this compile failure IS the RED.
  </behavior>
  <action>Run `cd backend && go get github.com/pgvector/pgvector-go@v0.4.1` (updates go.mod/go.sum). Create `pgvector_test.go` in package `vector` with the three tests above. The integration test must `t.Skip` when no DSN is reachable (document why). Do NOT write any implementation yet — run the tests and confirm they fail (missing pgvector.go / undefined symbols). Commit: `test(pgvector-store): add failing tests for PGVectorStore`.</action>
  <verify>
    <automated>cd backend && go test ./internal/ai/vector/ -run 'TestValidSchemaName|TestVectorDocumentMapping' 2>&1 | grep -i "undefined\|no required module"</automated>
  </verify>
  <done>go.mod pins pgvector-go v0.4.1; tests written; `go test ./internal/ai/vector/` fails to compile (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PGVectorStore (GREEN)</name>
  <files>backend/internal/ai/vector/pgvector.go (new)</files>
  <read_first>backend/internal/ai/vector/store.go, backend/internal/database/tenant/schema_db.go (plugin mechanics), backend/internal/middleware/tenant.go, backend/internal/middleware/audit.go, backend/internal/database/tenant/factory.go, backend/internal/ai/agents/academic_tutor.go (metadata contract line ~192)</read_first>
  <behavior>
    - Insert: dim guard (len(embedding) != store.embeddingDim → error, D-14); metadata requires _doc_id and _chunk_index (error when missing); rows carry embedding_model/model_version/chunking_version from the constructor; batch-create via schema-scoped gorm session.
    - Search: dim guard on query; limit clamped to 1..1000 (Rule B5); SQL `SELECT id, collection, 1 - (embedding <=> ?) AS score, document_id, chunk_index, text FROM {schema}.ai_vectors WHERE collection = ? ORDER BY embedding <=> ? LIMIT ?` (only the trusted schema name interpolated; everything else parameterized — Rule B7); results map back to Metadata{_doc_id, _chunk_index, _text}.
    - Delete: `DELETE FROM {schema}.ai_vectors WHERE collection = ? AND id IN ?` (GORM slice expansion, single statement — Rule B4); errors propagate (B9).
    - Close: no-op with a comment (shared pool), still returning nil.
  </behavior>
  <action>Implement `PGVectorStore`:
  - `type PGVectorStore struct { repoFactory *tenant.RepositoryFactory; embeddingModel, modelVersion, chunkingVersion string; embeddingDim int }` and `func NewPGVectorStore(repoFactory *tenant.RepositoryFactory, embeddingModel, modelVersion, chunkingVersion string, embeddingDim int) *PGVectorStore`.
  - `func (s *PGVectorStore) tenantFor(ctx context.Context) (*tenant.TenantRepositories, error)`: `repos, ok := ctx.Value(middleware.CtxKeyTenantRepos).(*tenant.TenantRepositories)`; if absent, fall back to `schoolID := middleware.GetSchoolIDFromCtx(ctx)`; if schoolID == 0 → error "no tenant context"; schema := fmt.Sprintf("school_%d", schoolID); validate with `validSchemaName` (regexp `^school_[0-9]+$`); `s.repoFactory.ForSchoolSchema(ctx, schoolID, schema)`. NEVER use the core DB for these queries (Rule B8).
  - Insert: build row struct (id, collection, embedding pgvector.Vector, document_id, chunk_index, text, embedding_model, model_version, chunking_version); `repos.TenantDB().Table("ai_vectors").Create(&rows)` (SchemaTablePrefix prefixes the table — same schema as Search); return the error (B9 mutation).
  - Search: dim guard; `repos.SchemaName()` builds the qualified table name; `repos.TenantDB().Raw(sql, pgvector.NewVector(query), collection, pgvector.NewVector(query), limit).Scan(&results)`; map to `[]SearchResult` preserving `_text`/`_doc_id`/`_chunk_index` keys; clamp limit to [1,1000], default 10.
  - Delete: `repos.TenantDB().Exec("DELETE FROM "+schema+".ai_vectors WHERE collection = ? AND id IN ?", collection, ids)`; propagate error.
  - Close: `return nil` with comment "shared gorm pool; nothing to close".
  - Context propagation throughout (Rule B2); no fmt.Printf/log (Rule B3); no `_` error discards (Rule B1).
  Run tests: unit + integration (`PGVECTOR_TEST_DSN` set from backend/.env values, schema school_1 must already exist with ai_vectors from plan 02-03). Commit: `feat(pgvector-store): implement PGVectorStore`.</action>
  <verify>
    <automated>cd backend && go vet ./internal/ai/vector/ && go test ./internal/ai/vector/ -run 'TestValidSchemaName|TestVectorDocumentMapping' && PGVECTOR_TEST_DSN="$(grep -E '^DB_' .env | tr '\n' ' ')" go test ./internal/ai/vector/ -run TestPGVectorStoreIntegration -v</automated>
  </verify>
  <done>All 4 Store methods implemented; unit + integration tests green; HNSW operator (`<=>`) used in Search ORDER BY (no seq-scan fallback); agent metadata contract preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RAG pipeline → PGVectorStore | Collection strings + query vectors cross from untrusted request scope |
| PGVectorStore → tenant DB | All SQL executes against a schema scoped by context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PGV-03-01 | S | tenantFor() | mitigate | Schema derived ONLY from ctx repos / validated schoolID; error when schoolID==0 or schema fails ^school_[0-9]+$; core DB never used (Rule B8) |
| T-PGV-03-02 | I | Search/Delete SQL | mitigate | Collection/embedding/ids/limit always parameterized; only trusted schema name interpolated (Rule B7) |
| T-PGV-03-03 | D | Insert/Search dim guard | mitigate | Fail-fast when embedding length != embeddingDim (D-14) — no silent truncation/padding |
| T-PGV-03-04 | D | Search limit | mitigate | Clamp to 1..1000 (Rule B5) |
| T-PGV-03-05 | D | HNSW bypass (seq-scan) | mitigate | ORDER BY keeps raw `embedding <=> ?` so `vector_cosine_ops` HNSW index applies; integration test asserts results + plan 07 verify EXPLAIN shows Index Scan |
</threat_model>

<verification>
- `go vet ./internal/ai/vector/` clean; unit tests pass; integration test passes against school_1 (table from plan 02-03).
- RAG pipeline + agents untouched: `git -C backend diff --name-only -- internal/ai/rag internal/ai/agents` is empty.
</verification>

<success_criteria>
- PGVectorStore is interface-conformant with the Qdrant implementation (same method set; same collection-string semantics).
- Tenancy resolved from context per request; every query lands in the correct `school_{id}` schema.
- `score = 1 - distance`; `_text` metadata preserved for agents.
- Dimension mismatches error, never silently write.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pgvector-migration/02-04-SUMMARY.md`
</output>
