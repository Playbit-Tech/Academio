---
phase: 02-pgvector-migration
plan: 05
type: execute
wave: 3
depends_on: [02-01, 02-03]
files_modified:
  - backend/cmd/copy-qdrant-vectors/main.go
autonomous: true
requirements: [PGV-05]
must_haves:
  truths:
    - "A Qdrant → pgvector copy tool exists with count/dimension/distance-semantics parity asserts"
    - "The tool never writes outside a validated school_{id} schema"
    - "It is a safe no-op when Qdrant is absent (current dev environment)"
  artifacts:
    - path: "backend/cmd/copy-qdrant-vectors/main.go"
      provides: "Qdrant → pgvector data migration CLI with --verify parity asserts"
      contains: "similarity = 1 - distance"
  key_links:
    - from: "copy tool"
      to: "ai_vectors"
      via: "same metadata mapping + dim guard as PGVectorStore (plan 02-04)"
    - from: "--verify"
      to: "parity asserts"
      via: "count equality + |qdrant_similarity - pgvector_score| <= 0.001 + dimension equality"
---

<objective>
Build and run a Qdrant → pgvector data migration tool (`backend/cmd/copy-qdrant-vectors`) that copies collections into per-tenant `ai_vectors` tables with parity verification (PGV-05).

Purpose: Current risk is LOW (no live Qdrant collections in dev — confirmed in RESEARCH), so the tool's realistic job today is to exist, compile, and safely no-op — while being fully correct the moment a Qdrant instance holds data. Parity is asserted with the same `similarity = 1 - distance` cosine semantics used by the pgvector store, so a cutover can never silently change ranking.
Output: Runnable CLI with `--dry-run`/`--verify`, schema guard, parity asserts, and documented usage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pgvector-migration/02-RESEARCH.md (low risk: no live collections; parity semantics)
@.planning/phases/02-pgvector-migration/02-CONTEXT.md (D-08, D-13)

<interfaces>
From backend/cmd/copy-tenant-data/main.go (pattern to mirror):
- Flags style: `--school-id`, `--concurrency` (default 3), `--verify`, dry-run; uses tenant factory + SchemaDB for schema-scoped writes.
From backend/internal/ai/vector/qdrant.go (old config for CLI defaults):
- Qdrant default URL `http://localhost:6333`, API key empty, timeout 30s.
From backend/internal/ai/vector/store.go:
- Metadata keys `_doc_id`, `_chunk_index`, `_text`; `VectorDocument{ID, Embedding, Metadata}`.
From RESEARCH:
- Score parity: pgvector `<=>` returns cosine DISTANCE (lower=better); Qdrant returns similarity. Convert `score = 1 - distance` in both the store (plan 02-04) and this tool's asserts.
- Qdrant HTTP API used via net/http (NO new Go dependency): `GET /collections`, `POST /collections/{name}/points/scroll` (page_size 256) → `{result: {points: [{id, payload, vector}]}}`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the copy tool with parity asserts (PGV-05)</name>
  <files>backend/cmd/copy-qdrant-vectors/main.go (new)</files>
  <read_first>backend/cmd/copy-tenant-data/main.go (flags + verify pattern), backend/internal/ai/vector/store.go, backend/internal/ai/vector/qdrant.go (config), backend/internal/database/tenant/factory.go</read_first>
  <action>Create `backend/cmd/copy-qdrant-vectors/main.go`:
  - Flags: `--qdrant-url` (default `http://localhost:6333`), `--qdrant-api-key` (default ""), `--qdrant-timeout` (default 30s), `--school-id` (required), `--concurrency` (default 3), `--dry-run`, `--verify`.
  - Schema guard (MANDATORY): schoolID → schema `school_{id}` must match `^school_[0-9]+$`; verify the schema EXISTS in `information_schema.schemata` (query via tenant factory); error and exit non-zero otherwise. NEVER write to a missing or mismatched schema (T-PGV-05-01).
  - Qdrant client via net/http (no new dependency): `GET /collections` → for each collection, scroll `POST /collections/{name}/points/scroll` page_size 256, reading `id`, `payload` (_doc_id/_chunk_index/_text), `vector`.
  - Dim guard: every vector must be exactly 1536 (embeddingDim constant matching AI_EMBEDDING_DIM — D-14); collect all mismatches and fail with ONE message (Rule B9 batch).
  - Writes: same mapping as PGVectorStore — `document_id` from `_doc_id`, `chunk_index` from `_chunk_index` (required), `text` from `_text`, plus `embedding_model`/`model_version`/`chunking_version` (from flags or constants "text-embedding-3-small"/"v1"/"v1"), into `{schema}.ai_vectors` via the tenant factory's schema-scoped session.
  - `--verify` parity asserts per collection: (a) count equality (Qdrant points == pgvector rows for that collection+schema), (b) dimension equality, (c) for top-k (k=10) points: `|qdrant_similarity - pgvector_score| <= 0.001` where pgvector_score comes from `SELECT 1 - (embedding <=> $1) AS score ... ORDER BY embedding <=> $1`. Any assert failure → collect and exit non-zero with all failures reported.
  - Safe no-op: if Qdrant is unreachable (connection refused / timeout), `logger.Warn("qdrant not reachable — nothing to copy (no-op)")` and exit 0 — this is the expected dev-environment outcome (no qdrant container running).
  - `--dry-run`: print collections + point counts + target schema without writing.
  - Use pkg/logger only (Rule B3); no multi-statement Exec (Rule B4). Header comment MUST document: (a) usage + the no-op behavior; (b) the 3-key parity contract — Qdrant payload keys `_doc_id`/`_chunk_index`/`_text` map 1:1 to the `document_id`/`chunk_index`/`text` columns; metadata is FLATTENED and no other payload keys are preserved (jsonb intentionally omitted per agent discretion under D-09; matches the ai_vectors DDL from plan 02-03 and the PGVectorStore mapping from plan 02-04). The `--verify` parity asserts depend on this exact mapping, so the contract lives at the top of the file.</action>
  <verify>
    <automated>cd backend && go build ./cmd/copy-qdrant-vectors/</automated>
  </verify>
  <done>Tool compiles; schema guard, dim guard, parity asserts, and no-op path implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Run and validate the tool (PGV-05)</name>
  <files>backend/cmd/copy-qdrant-vectors/main.go (usage header finalized)</files>
  <read_first>backend/cmd/copy-tenant-data/main.go (run pattern), docker ps (confirm no qdrant container)</read_first>
  <action>Run `cd backend && go run ./cmd/copy-qdrant-vectors --school-id 1 --dry-run` then `go run ./cmd/copy-qdrant-vectors --school-id 1 --verify`. In the current dev environment Qdrant is NOT running → expect the no-op path (warn log, exit 0). Confirm exit code 0 and that no rows were written (`SELECT count(*) FROM school_1.ai_vectors` still 0 unless plan 02-04 integration seeded it). Finalize the file-header usage comment (flags, examples, no-op semantics). Commit: `feat(copy-qdrant-vectors): qdrant to pgvector migration tool`.</action>
  <verify>
    <automated>cd backend && go run ./cmd/copy-qdrant-vectors --school-id 1 --verify; echo "exit=$?"</automated>
  </verify>
  <done>Tool runs to exit 0 (no-op in dev); usage documented; no spurious writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Qdrant HTTP API → tool | Unvalidated remote payloads (ids, payloads, vectors) cross in |
| tool → tenant schema | Writes execute against a school_{id} schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PGV-05-01 | S | schema guard | mitigate | Schema must match ^school_[0-9]+$ AND exist in information_schema; error + non-zero exit otherwise (never auto-create/write elsewhere) |
| T-PGV-05-02 | I | payload mapping | mitigate | _doc_id/_chunk_index required (no silent defaults); dimension guard 1536 with batch error collection (Rule B9) |
| T-PGV-05-03 | D | parity | mitigate | --verify asserts count/dimension/|sim|Δ|≤0.001 with 1 - distance semantics; failures exit non-zero with all messages |
| T-PGV-05-04 | S | credentials | accept | Local dev CLI; API key via flag/env, never committed; Qdrant absent in dev → no-op path |
</threat_model>

<verification>
- `go build ./cmd/copy-qdrant-vectors/` passes.
- `go run ./cmd/copy-qdrant-vectors --school-id 1 --verify` exits 0 (no-op) in dev.
- `docker ps` shows no qdrant container (expected).
</verification>

<success_criteria>
- Copy tool exists, compiles, and is safe to run with zero Qdrant data (no-op).
- Parity asserts (count/dimension/similarity 1 - distance) in place for a real cutover.
- No writes outside validated school_{id} schemas.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pgvector-migration/02-05-SUMMARY.md`
</output>
