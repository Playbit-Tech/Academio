---
phase: 02-pgvector-migration
plan: 06
type: execute
wave: 4
depends_on: [02-04, 02-05]
files_modified:
  - backend/internal/config/config.go
  - backend/internal/config/config_test.go
  - backend/.env.example
  - backend/internal/router/setup.go
  - backend/docker-compose.yml
  - backend/deploy/k8s/base/configmap.yaml
  - backend/deploy/k8s/base/kustomization.yaml
  - backend/deploy/k8s/base/qdrant-deployment.yaml
  - backend/deploy/k8s/base/qdrant-service.yaml
  - backend/deploy/k8s/base/qdrant-pvc.yaml
autonomous: true
requirements: [PGV-06]
must_haves:
  truths:
    - "AI config no longer references Qdrant; the pgvector DSN is required and validated at startup"
    - "The RAG pipeline uses PGVectorStore (built behind the same interface)"
    - "Startup validates the embedder's output dimension against the column type and fails fast (D-14)"
    - "Qdrant is retired from docker-compose and k8s manifests"
  artifacts:
    - path: "backend/internal/config/config.go"
      provides: "AI_PGVECTOR_DSN required config (no default)"
      contains: "PgVectorDSN"
    - path: "backend/internal/router/setup.go"
      provides: "PGVectorStore wiring replacing QdrantStore + dimension probe"
      contains: "NewPGVectorStore"
    - path: "backend/docker-compose.yml"
      provides: "Qdrant service removed"
      not_contains: "qdrant"
  key_links:
    - from: "setup.go NewPGVectorStore"
      to: "rag.NewPipeline"
      via: "chunker + embedder unchanged (zero RAG/agent changes)"
    - from: "config.validate()"
      to: "AI_PGVECTOR_DSN"
      via: "unconditional required-field check (mirrors AI_ENGINE_URL precedent)"
    - from: "embedder probe"
      to: "cfg.AI.EmbeddingDim"
      via: "logger.Fatal on mismatch (fail-fast, cmd/server precedent)"
---

<objective>
Swap the AI config from `AI_QDRANT_*` to a pgvector DSN, wire the router to build `PGVectorStore` (with the D-14 startup dimension probe), and retire Qdrant from compose and k8s (PGV-06).

Purpose: Complete the cutover so the platform runs entirely on pgvector. The swap and the RAG wiring land in the SAME change so there is never a window where the RAG pipeline is silently disabled (RESEARCH pitfall 6). Startup must fail fast on a missing DSN and on embedder/column dimension mismatch (D-14, Rule B12).
Output: Qdrant fully removed from config, compose, and k8s; RAG initialized over pgvector.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pgvector-migration/02-RESEARCH.md (pitfall 6: no silent-disable window; DSN swap)
@.planning/phases/02-pgvector-migration/02-CONTEXT.md (D-10, D-12, D-14)

<interfaces>
From backend/internal/config/config.go:
- `AIConfig` fields today: `QdrantURL env:"AI_QDRANT_URL"` (default `http://localhost:6333`, line ~316), `QdrantAPIKey env:"AI_QDRANT_API_KEY"`, `QdrantTimeout env:"AI_QDRANT_TIMEOUT"` (default 30), `EmbeddingDim env:"AI_EMBEDDING_DIM"` (added in plan 02-01), `EngineURL`, `EngineToken`.
- `validate()` (line ~408): `AI_ENGINE_URL`/`AI_ENGINE_TOKEN` are required UNCONDITIONALLY (Phase 1 precedent) — mirror this for `AI_PGVECTOR_DSN`.
From backend/internal/router/setup.go:
- `func NewRouter(cfg *config.Config, db *gorm.DB, rdb *redis.Client) (*gin.Engine, func(context.Context) error, *scheduler.CronRunner)` (line 139) — NO error return; fatal failures use `logger.Fatal` (precedent cmd/server/main.go lines 85/111/128).
- In scope inside NewRouter: `repoFactory` (`tenant.NewRepositoryFactory(db)` + `middleware.SetTenantDBFactory(repoFactory)` at lines ~200-201), `aiProvider` (lines 169-181, `ai.NewProvider(aiCfg)`).
- Current store block (lines ~690-720): `if cfg.AI.QdrantURL != "" { vecStore := vector.NewQdrantStore(...); chunker...; embedder := rag.EmbedderFunc(...GenerateEmbeddings...); ragPipeline = rag.NewPipeline(chunker, embedder, vecStore); logger.Info("RAG pipeline initialized", "qdrant_url", ...) }`.
From backend/internal/ai/vector/pgvector.go (plan 02-04):
- `vector.NewPGVectorStore(repoFactory, embeddingModel, modelVersion, chunkingVersion string, embeddingDim int) *PGVectorStore`.
From backend/docker-compose.yml:
- Qdrant service block lines ~34-48 (`image: qdrant/qdrant:latest`, container academio-qdrant, ports 6333/6334, volume qdrant_data, curl healthcheck) + `qdrant_data` under top-level `volumes:`.
From backend/deploy/k8s/base/:
- configmap.yaml line ~18: `AI_QDRANT_URL: http://qdrant:6333`; kustomization.yaml references qdrant-deployment.yaml, qdrant-service.yaml, qdrant-pvc.yaml.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap AI config from Qdrant to pgvector DSN (PGV-06)</name>
  <files>backend/internal/config/config.go, backend/internal/config/config_test.go, backend/internal/ai/config.go, backend/.env.example</files>
  <read_first>backend/internal/config/config.go (AIConfig, defaults ~316, validate() ~408), backend/internal/config/config_test.go (fixture lines ~141-142, negative-test pattern ~150-186), backend/internal/ai/config.go (AIServiceConfig struct + FromAppConfig), backend/.env.example (lines ~102-104 AI_QDRANT_*, ~111-112 AI_ENGINE_*)</read_first>
  <action>In `config.go`: DELETE `QdrantURL`, `QdrantAPIKey`, `QdrantTimeout` fields and their `getEnv`/`getInt` defaults; ADD `PgVectorDSN string` with env tag `AI_PGVECTOR_DSN` and NO default (Rule B6/B12). In `validate()`: if `PgVectorDSN == ""` → `fmt.Errorf("AI_PGVECTOR_DSN is required")` — unconditional, mirroring the `AI_ENGINE_URL` precedent (no conditional gating on AI.Enabled; keeps the server from starting without a vector backend). Keep `EmbeddingDim` validation from plan 02-01. In `config_test.go`: remove Qdrant fields from the valid fixture and add `PgVectorDSN: "postgres://postgres:postgres@localhost:5432/academio?sslmode=disable"`; add `t.Run("missing AI_PGVECTOR_DSN rejected")` (fixture without it → `assert.ErrorContains(t, err, "AI_PGVECTOR_DSN")`). In `.env.example`: replace the commented `AI_QDRANT_URL`/`AI_QDRANT_API_KEY`/`AI_QDRANT_TIMEOUT` lines with `AI_PGVECTOR_DSN=postgres://postgres:postgres@localhost:5432/academio?sslmode=disable` (kept adjacent to AI_ENGINE_*). Executor: also set `AI_PGVECTOR_DSN` in backend/.env (gitignored — do not commit). **MANDATORY — update `backend/internal/ai/config.go` in the SAME task (compile-breaker):** remove the `Qdrant vector.QdrantConfig` field from the `AIServiceConfig` struct (line ~26) and remove the entire `Qdrant: vector.QdrantConfig{URL: cfg.QdrantURL, APIKey: cfg.QdrantAPIKey, Timeout: time.Duration(cfg.QdrantTimeout) * time.Second}` mapping from `FromAppConfig` (lines ~44-48) — otherwise deleting `QdrantURL/QdrantAPIKey/QdrantTimeout` from `config.AIConfig` fails to compile (Rule B1: build must pass). After removing the field+mapping, the imports `"time"` and `"github.com/playbits/schoolcare-v2/internal/ai/vector"` in `config.go` become UNUSED — remove both import lines too (Go rejects unused imports). Keep `EngineURL`/`EngineToken` fields and mappings unchanged. Run `cd backend && go build ./...` (full build — catches the ai/config.go break) then `go test ./internal/config/ -v`.</action>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/config/ -v && test 0 -eq $(grep -rn "Qdrant" internal/config/ internal/ai/config.go | wc -l)</automated>
  </verify>
  <done>Config references zero Qdrant fields; AIServiceConfig has no Qdrant field/mapping and compiles (unused time/vector imports removed); AI_PGVECTOR_DSN required with negative test; .env.example documents the DSN.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PGVectorStore + dimension probe into the router (PGV-06, D-14)</name>
  <files>backend/internal/router/setup.go</files>
  <read_first>backend/internal/router/setup.go (lines 139-295 scope, lines ~690-720 store block), backend/internal/ai/vector/pgvector.go (constructor), backend/cmd/server/main.go (logger.Fatal precedent)</read_first>
  <action>Replace the `if cfg.AI.QdrantURL != ""` block in `NewRouter`: keep the SAME gate shape but on the new DSN — `if cfg.AI.PgVectorDSN != ""` — and inside it: `vecStore := vector.NewPGVectorStore(repoFactory, "text-embedding-3-small", "v1", "v1", cfg.AI.EmbeddingDim)` (repoFactory is in scope; version strings are informational metadata columns; embeddingModel MUST match the canonical model from D-01). Keep the chunker and the `rag.EmbedderFunc` wrapping `aiProvider.GenerateEmbeddings` UNCHANGED (zero RAG/agent changes, D-05/D-06). IMMEDIATELY AFTER building the embedder, add the D-14 startup probe: `probeVec, err := embedder(context.Background(), []string{"academio embedding dimension probe"})`; if err != nil OR `len(probeVec[0]) != cfg.AI.EmbeddingDim` → `logger.Fatal("Embedding dimension validation failed", "got", n, "want", cfg.AI.EmbeddingDim, "model", "text-embedding-3-small")` (fail-fast — NewRouter has no error return; this matches the cmd/server logger.Fatal precedent). Update the success log: `logger.Info("RAG pipeline initialized", "pgvector_dsn", cfg.AI.PgVectorDSN, "chunker", rag.StrategyFixedSize, "embedding_dim", cfg.AI.EmbeddingDim)`. Remove the "qdrant_url" log field. `cd backend && go build ./... && go vet ./internal/router/` then restart the server and confirm the RAG pipeline init log appears.</action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./internal/router/ && grep -n "NewQdrantStore\|qdrant_url" internal/router/setup.go | wc -l</automated>
  </verify>
  <done>setup.go builds PGVectorStore behind the interface, logs pgvector_dsn, fails fast on dimension mismatch; zero NewQdrantStore/qdrant_url references remain.</done>
</task>

<task type="auto">
  <name>Task 3: Retire Qdrant from compose and k8s (PGV-06)</name>
  <files>backend/docker-compose.yml, backend/deploy/k8s/base/configmap.yaml, backend/deploy/k8s/base/kustomization.yaml, backend/deploy/k8s/base/qdrant-deployment.yaml (delete), backend/deploy/k8s/base/qdrant-service.yaml (delete), backend/deploy/k8s/base/qdrant-pvc.yaml (delete)</files>
  <read_first>backend/docker-compose.yml (qdrant service lines ~34-48 + volumes), backend/deploy/k8s/base/kustomization.yaml, backend/deploy/k8s/base/configmap.yaml</read_first>
  <action>These are mechanical YAML/config/delete edits — deliberately kept as ONE retirement task (no behavioral code changes; splitting would not improve clarity). 1) `backend/docker-compose.yml`: remove the `qdrant:` service block and the `qdrant_data:` volume entry. 2) k8s base: remove the qdrant-deployment.yaml/qdrant-service.yaml/qdrant-pvc.yaml entries from `kustomization.yaml`; `git rm` the three qdrant yaml files; in `configmap.yaml` remove `AI_QDRANT_URL: http://qdrant:6333` and add `AI_PGVECTOR_DSN: postgres://postgres:postgres@academio-pg:5432/academio?sslmode=disable` (cluster-internal DSN) plus `AI_EMBEDDING_DIM: "1536"`. 3) Retirement-scoped sweep (NOT a repo-wide zero-qdrant gate — qdrant.go is intentionally retained, see whitelist below). Sweep these surfaces ONLY:
  - `grep -rin "AI_QDRANT_" backend/ --exclude-dir=vendor --exclude-dir=node_modules` → must return ZERO matches (config, .env.example, k8s configmap are the only legitimate surfaces and are all cleaned in this task).
  - `backend/docker-compose.yml`: no `qdrant:` service, no `qdrant_data:` volume entry (grep `qdrant` in this file → 0).
  - `backend/deploy/k8s/`: no qdrant resources in base or any overlay (`grep -ril "qdrant" deploy/k8s/` → 0).
  - `backend/.env` (gitignored, local): remove any lingering `AI_QDRANT_*` vars so the sweep count is clean — they are replaced by `AI_PGVECTOR_DSN` (set in Task 1).
  - Makefile / README / docs / .github / other config: any `qdrant`/`AI_QDRANT` hit → update to pgvector wording or annotate as historical.
  **EXPLICITLY WHITELISTED — do NOT touch, do NOT count as violations:** (a) `backend/internal/ai/vector/qdrant.go` — RETAINED as the behavioral reference for pgvector parity per D-05 (38 qdrant refs); final removal is Phase 7 RET-02, NOT this phase; (b) the package comment in `backend/internal/ai/vector/store.go` (line 2, "Qdrant-backed implementation" — remains accurate while qdrant.go exists); (c) `backend/cmd/copy-qdrant-vectors/` — the migration tool from plan 02-05, which by design talks to a Qdrant HTTP API. These three are Go code / the migration tool, not config surfaces, so the sweep's zero-match expectation does not apply to them.
  Then validate: `cd backend && docker compose config --quiet` and, if kustomize/kubectl is available, `kubectl kustomize deploy/k8s/base` shows no qdrant resources. Commit: `feat(pgvector): cut over to pgvector, retire qdrant`.</action>
  <verify>
    <automated>cd backend && test 0 -eq $(grep -rin "AI_QDRANT_" --exclude-dir=vendor --exclude-dir=node_modules . | wc -l) && grep -ic "qdrant" docker-compose.yml | xargs test 0 -eq && test 0 -eq $(grep -ril "qdrant" deploy/k8s/ | wc -l) && ls internal/ai/vector/qdrant.go cmd/copy-qdrant-vectors/main.go && docker compose config --quiet</automated>
  </verify>
  <done>Zero `AI_QDRANT_` env/config references anywhere; compose has no qdrant service/volume; k8s base+overlays have no qdrant resources; whitelisted Go files (qdrant.go behavioral reference per D-05, copy tool) still present; compose valid; server starts with RAG pipeline over pgvector.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env/.env.example → config.validate() | Missing/malformed DSN crosses into startup |
| embedder output → vector(1536) column | Runtime embedding dimension vs declared column type |
| setup.go wiring → RAG pipeline | Misconfiguration silently disables search |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PGV-06-01 | D | config.validate() | mitigate | AI_PGVECTOR_DSN required unconditionally (no default — Rule B6/B12); negative test covers missing value |
| T-PGV-06-02 | D | setup.go probe | mitigate | Startup embedder probe; mismatch → logger.Fatal (D-14) — never starts with wrong dim |
| T-PGV-06-03 | D | RAG gate | mitigate | DSN swap + wiring in SAME change — no window with pipeline silently disabled (RESEARCH pitfall 6) |
| T-PGV-06-04 | I | config remnants | mitigate | Retirement-scoped sweep: zero `AI_QDRANT_` env/config refs, zero compose qdrant service/volume, zero k8s qdrant resources; Go behavioral reference (qdrant.go) + copy tool whitelisted per D-05 — final removal is Phase 7 RET-02 |
</threat_model>

<verification>
- `go test ./internal/config/ -v` passes; `go build ./...` passes (includes internal/ai/config.go after Qdrant removal); `grep -rn "Qdrant" internal/config/ internal/ai/config.go` == 0.
- `go build ./...` + `go vet ./internal/router/` pass; server restart logs "RAG pipeline initialized" with pgvector_dsn.
- Retirement sweep: `grep -rin "AI_QDRANT_" backend/` (excluding vendor/node_modules) == 0; no qdrant service/volume in compose; no qdrant resources under deploy/k8s/ (base + overlays).
- Whitelisted (still present, expected): `internal/ai/vector/qdrant.go` (behavioral reference per D-05), `store.go` package comment, `cmd/copy-qdrant-vectors/` (plan 02-05 tool).
- `docker compose config --quiet` valid; `kubectl kustomize deploy/k8s/base` has no qdrant resources.
</verification>

<success_criteria>
- Config fully swapped to AI_PGVECTOR_DSN; Qdrant retired from compose + k8s.
- RAG pipeline initialized over PGVectorStore with zero RAG/agent changes.
- Startup fails fast on missing DSN or embedder/column dimension mismatch.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pgvector-migration/02-06-SUMMARY.md`
</output>
