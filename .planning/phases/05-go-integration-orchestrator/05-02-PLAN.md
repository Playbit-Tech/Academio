---
phase: 05-go-integration-orchestrator
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .planning/phases/05-go-integration-orchestrator/05-02-PLAN.md
autonomous: true
requirements: [INT-04]
user_setup: []

must_haves:
  truths:
    - "Python providers appear in ModelRouter alongside Gemini/OpenAI"
    - "Two-level routing: Go selects python:anthropic etc.; Python makes internal provider choices"
    - "Strict timeout layering: Go→Python (35s) > Python→LLM (30s) before Python calls"
    - "Error classification: 429→failover, 5xx/timeout→retry once→failover, 4xx→permanent"
    - "Streaming failover: if Python provider fails before first byte, Go switches to another"
    - "All providers write unified cost ledger entries via ai.CalculateCost"
    - "Embeddings/CountTokens use primary provider (Gemini/OpenAI), not Python entries"
  artifacts:
    - path: "backend/internal/ai/gateway.go"
      provides: "Extensions to NewProvider that add pythonProvider entries to ModelRouter"
      min_lines: 40
    - path: "backend/internal/ai/engine/python_provider.go"
      provides: "pythonProvider adapter implementing ai.Provider for Python engines"
      contains: "type pythonProvider struct"
    - path: "backend/internal/ai/engine/embed_spike_test.go"
      provides: "Embed method for Python provider to satisfy ai.Provider interface"
      contains: "func (p *pythonProvider) GenerateEmbeddings"
    - path: "backend/internal/ai/gateway_test.go"
      provides: "Tests for provider selection including Python entries"
      contains: "TestNewProvider_withPythonEntries"
  key_links:
    - from: "backend/internal/ai/gateway.go"
      to: "backend/internal/ai/engine/python_provider.go"
      via: "NewProvider creates pythonProvider instances via client and EngineClient"
      pattern: "newPythonProvider\|ProviderType\("python"")"
    - from: "backend/internal/ai/engine/python_provider.go"
      to: "backend/internal/ai/engine/client.go"
      via: "pythonProvider uses EngineClient for API calls"
      pattern: "client.Chat\|client.ChatStream"
    - from: "backend/internal/ai/engine/python_provider.go"
      to: "backend/internal/ai/gateway.go"
      via: "pythonProvider implements Provider interface"
      pattern: "func (p \*pythonProvider) GenerateText"
---

<objective>
Add Python providers (anthropic, deepseek, openrouter, azure-openai, ollama) to the existing Go ModelRouter, enabling two-level routing: Go platform-level failover + Python internal provider selection. Ensure embeddings and count-tokens use primary providers (Gemini/OpenAI), not Python entries. Implement pythonProvider adapter to satisfy ai.Provider interface, and strict timeout/error classification at Go boundary.

Purpose: Extend ModelRouter to include Python entries, shipping the two-level routing contract (INT-04) required for Phase 4 workloads. This is the gateway layer for orchestrator's provider calls to the five Python engines.
Output: Python entries in ModelRouter, pythonProvider adapter, Embed method in EngineClient, and Go-level routing/protection for Python.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-go-integration-orchestrator/05-CONTEXT.md
@.planning/phases/05-go-integration-orchestrator/05-RESEARCH.md
@backend/internal/ai/model_router.go (lines 17-130)
@backend/internal/ai/gateway.go (lines 116-135)
@backend/internal/ai/engine/client.go (lines 1-130)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pythonProvider Adapter</name>
  <files>backend/internal/ai/engine/python_provider.go</files>
  <action>
    Create pythonProvider adapter implementing ai.Provider interface. Contains fields: client engine.EngineClient, name string.
    
    Implement methods:
    - GenerateText: call client.Chat with name-prefixed model, translate ChatResponse to ai.Response, normalize usage payload (provider, model, input/output tokens, cost) into ai.CostConfig shape for unified ledger
    - GenerateTextStream: call client.ChatStream with context, return ai.GenerateTextStream to preserve heartbeat and in-band errors; after first byte, no failover
    - GenerateEmbeddings: implement by calling new Embed method on EngineClient (must add to client.go)
    - CountTokens: local estimation via ai.EstimateTokens (no Python token endpoint)
    - Close: no-op
  </action>
  <verify>
    <automated>go test ./backend/internal/ai/engine -run TestPythonProvider -v</automated>
  </verify>
  <done>
    pythonProvider adapter compiles, implements ai.Provider interface, correctly translates Python responses to unified cost shape, supports streaming with failover control, and routes embeddings through EngineClient.Embed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Embed Method to EngineClient</name>
  <files>backend/internal/ai/engine/client.go</files>
  <action>
    Add new Embed(ctx context.Context, req engine.EmbedRequest) (*engine.EmbedResponse, error) method to EngineClient. This method calls Python /v1/embed endpoint via HTTP client with service token. Returns embeddings matching the locked canonical model/dimension (1536-dim).
  </action>
  <verify>
    <automated>go test ./backend/internal/ai/engine -run TestEmbedMethod -v</automated>
  </verify>
  <done>
    EngineClient.Embed method compiles, calls /v1/embed with proper headers, returns EmbedResponse with model/dimension/validation matching Phase 2.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire Python Providers into ModelRouter</name>
  <files>backend/internal/ai/gateway.go</files>
  <action>
    Extend NewProvider to add Python provider entries when AI_ENGINE_URL and AI_ENGINE_TOKEN are configured. For each provider (anthropic, deepseek, openrouter, azure-openai, ollama), create pythonProvider with EngineClient and add to entries slice with ProviderType "python:{name}".
    
    Ensure order: append after Gemini/OpenAI so embeddings/count-tokens use primary provider.
    
    Update gateway.go to expose pythonProvider type.
    
    ModelRouter resolveProvider already contains `strings.Contains(model, pType)` matching; python:anthropic will match model prefixes containing this string.
  </action>
  <verify>
    <automated>go test ./backend/internal/ai -run TestNewProvider_withPythonEntries -v</automated>
  </verify>
  <done>
    NewProvider creates ModelRouter with Python entries appended after Gemini/OpenAI. Provider selection matches via strings.Contains(model, "python:{name}"). Embeddings/count-tokens delegate to providers[0]. All unit tests pass for provider selection and routing.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update Streaming Handler to Use Provider Router</name>
  <files>backend/internal/modules/ai/stream.go, backend/internal/modules/ai/handler.go</files>
  <action>
    Modify stream.go StreamChat handler to use aiProvider (ModelRouter) instead of engine client directly. Call aiProvider.GenerateTextStream with context, apply existing heartbeat and in-band error handling.
    
    Update handler.go to route /ai/chat/stream through orchestrator (already protected by orchestrator middleware) and use provider router for failover.
  </action>
  <verify>
    <automated>go test ./backend/internal/modules/ai -run TestStreamChatThroughProvider -v</automated>
  </verify>
  <done>
    Streaming routes through ModelRouter, enabling D-16 failover-before-first-byte. Heartbeat, buffer, and error handling preserved. End-to-end test verifies streaming works through both primary and Python providers with proper failover.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted input crosses here (AI endpoint requests) |
| API→orchestrator | Trust boundary where enforcement occurs |
| orchestrator→Python service | External service call requiring protection |
| Python service→LLM | External API call (handled by Python) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | S (Spoofing) | Provider type strings | mitigate | ProviderType values are fixed in code (python:anthropic, etc.) — no user input |
| T-05-02 | T (Tampering) | Model matching | mitigate | Model resolution uses strings.Contains(model, pType) — provider type is controlled by Go code, not user |
| T-05-03 | I (Information Disclosure) | ModelRouter entries | mitigate | ModelRouter exists in Go code, no secrets stored; pythonProvider stores only client config (url/token from env) |
| T-05-04 | D (Denial of Service) | Provider failover | mitigate | Circuit breaker state tracking prevents repeated failures; error classification prevents retry loops |
| T-05-05 | E (Elevation of Privilege) | Python client config | mitigate | Credentials from environment variables (Rule B6), never hardcoded; token only in environment |
| T-05-06 | D (Denial of Service) | Embedding routing bypass | mitigate | Embeddings/count-tokens always use providers[0]; Python entries after gemini/openai prevents accidental routing |
</threat_model>

<verification>
<automated>go test ./backend/internal/ai -run TestModelRouter_withPython -v</automated>
<automated>go test ./backend/internal/modules/ai -run TestAIEndpoints -v</automated>
</verification>

<success_criteria>
- ModelRouter includes python:anthropic, python:deepseek, python:openrouter, python:azure-openai, python:ollama entries after gemini/openai
- GenerateText/GenerateTextStream route through ModelRouter with proper timeout/error classification
- Embeddings and CountTokens use providers[0] (primary, non-Python) as required
- Streaming failover works: if Python provider fails before first byte, Go switches to another provider (D-16)
- All Go tests pass; Python codebase linting passes; no new Go dependencies
</success_criteria>

<output>
After completion, create .planning/phases/05-go-integration-orchestrator/05-02-PLAN.md
</output>