---
phase: 06-observability-security-testing
plan: 04
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - .planning/phases/06-observability-security-testing/06-04-PLAN.md
autonomous: true
requirements: [TES-01, SEC-01]
user_setup: []

must_haves:
  truths:
    - "Cross-tenant probe suite asserts ZERO cross-tenant hits across search, cache, reranking, agent tools, worker paths, and Python search/extract entry points"
    - "Kill-worker-mid-pipeline test asserts zero duplicate vectors and exactly one notification"
    - "Cross-tenant probes + kill-worker test run in CI on every deploy (integration-tests job paths/tags extended; Python probes run in rag-eval workflow)"
    - "Load/concurrency tests pass at 10x expected peak via existing k6 workflow"
    - "Phase 5 review hardening: MA-01 (per-request cost cap wired), MA-02 (ChatStream error classification), MA-03 (pythonProvider passes Temperature/MaxTokens)"
  artifacts:
    - path: "backend/internal/security/probes/probes_test.go"
      provides: "Cross-tenant adversarial probe tests"
      contains: "func TestCrossTenantIsolation"
    - path: "backend/internal/queue/kill_worker_test.go"
      provides: "Kill-worker-mid-pipeline test"
      contains: "func TestKillWorkerMidPipeline"
    - path: "ai-engine/tests/test_cross_tenant.py"
      provides: "Python-side adversarial cross-tenant probe tests"
      contains: "def test_cross_tenant_isolation"
    - path: "backend/.github/workflows/ci.yml"
      provides: "CI wiring for probes + kill-worker test"
      contains: "internal/security"
  key_links:
    - from: "backend/internal/security/probes/probes_test.go"
      to: "backend/internal/modules/ai/handler.go"
      via: "probes exercise search/cache endpoints cross-tenant"
      pattern: "search\|cache"
    - from: "backend/internal/queue/kill_worker_test.go"
      to: "backend/internal/queue/worker.go"
      via: "test drives asynq worker processing doc-ingest task"
      pattern: "TypeAIDocIngest\|asynq"
    - from: "backend/.github/workflows/ci.yml"
      to: "backend/internal/security/probes/probes_test.go"
      via: "integration-tests job runs the build-tagged probe package"
      pattern: "internal/security\|integration"
---

<objective>
Implement Phase 6 testing + Phase 5 hardening (TES-01, SEC-01): a hermetic cross-tenant probe suite (seeded school_1/school_2, adversarial queries across every retrieval entry point — Go AND Python search/extract — asserting ZERO cross-tenant hits), a kill-worker-mid-pipeline test (zero duplicate vectors, exactly one notification, with a companion handler ready-status guard so the assertion holds), wire the probes + kill-worker test into CI (integration-tests job + rag-eval workflow), extend the existing k6 load workflow to 10× peak, and fix the three Phase 5 review findings (MA-01 cost-cap dead code, MA-02 ChatStream error classification, MA-03 pythonProvider param passthrough).

Purpose: Prove tenant isolation in CI and close the Phase 5 review debt.
Output: Probes test package (Go + Python), kill-worker test + handler guard, CI wiring, k6 extension, three review fixes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-observability-security-testing/06-CONTEXT.md
@.planning/phases/05-go-integration-orchestrator/05-REVIEW.md
@backend/internal/modules/ai/handler.go
@backend/internal/modules/ai/service.go
@backend/internal/services/ai_orchestrator_service.go
@backend/internal/ai/engine/python_provider.go
@backend/internal/ai/engine/engine.go (StatusError)
@backend/internal/queue/worker.go
@backend/internal/queue/tasks.go
@backend/internal/middleware/tenant.go
@backend/internal/security/pii.go (06-02 output)
@backend/.github/workflows/load-test.yml
</context>

<threat_model>
- Cross-tenant leakage: adversarial queries must hit the REAL retrieval entry points (search, cache, rerank, agent tools, worker paths) with tenant-scoped context — probes must fail loudly (t.Fatal) on any cross-tenant hit; no test may silently swallow a leak.
- Worker kill race: kill-worker-mid-pipeline must be deterministic (use asynq task state/marker file to know the worker died mid-insert) to avoid flaky passes.
- Review fixes must not regress behavior: MA-02 error classification change must keep streaming contract (in-band errors after HTTP 200 per INT-01).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Cross-tenant probe suite</name>
  <files>backend/internal/security/probes/probes_test.go</files>
  <action>
    Create backend/internal/security/probes/probes_test.go (new package "probes" under backend/internal/security). This is a hermetic integration test package (build tag `//go:build integration` so it only runs in CI or explicitly) — check how existing integration tests are gated in backend/scripts/test_endpoint.sh or the load-test workflow; if they run with a build tag or env var (e.g. TEST_INTEGRATION=1), mirror that pattern.

    The suite:
    1. Seed two tenant schemas school_1 and school_2 (use the existing tenant provisioning helper or direct GORM migration for test schemas — check how middleware/tenant tests or scripts set up test schemas; if a test helper exists, reuse it).
    2. Insert distinct doc/chunk data in each tenant (school_1: "PRIVATE-FOR-SCHOOL-ONE-<uuid>", school_2: "PRIVATE-FOR-SCHOOL-TWO-<uuid>") into the pgvector vectors table and the docs table.
    3. Adversarial queries: for each retrieval entry point, query as school_1 and assert the response NEVER contains school_2's marker strings, and vice versa:
       - Search endpoint (GET/POST /api/v2/ai/search with tenant context)
       - Cache: verify cache keys are tenant-scoped — put a value under school_1 cache key, query school_2's cache key, assert miss
       - Reranking: any rerank path (if exists in the AI module; if none, assert the search path covers it)
       - Agent tools: the AI agent tool paths (ListAgents / any agent-search tool)
       - Worker paths: a queued doc-ingest task for school_1 must not be visible/processable as school_2
    4. Each adversarial assertion uses t.Fatal on leak. The suite skips (t.Skip) when the required DB/env (TEST_DB_DSN or the same AI_PGVECTOR_DSN pattern) is unavailable.

    Write helper functions: setupTestTenants(t), markerFor(tenant), assertNoCrossTenantLeak(t, resp, tenant).
  </action>
  <read_first>
    backend/internal/modules/ai/handler.go (search/cache/agent handlers)
    backend/internal/middleware/tenant.go (GetTenantDB, GetSchemaName, GetSchemaDB)
    backend/internal/services/ai_orchestrator_service.go (CacheKey — tenant-scoped keys)
    backend/internal/database/tenant/ (tenant provisioning/migration helpers for tests)
  </read_first>
  <acceptance_criteria>
    - test -f backend/internal/security/probes/probes_test.go
    - grep "func TestCrossTenantIsolation" backend/internal/security/probes/probes_test.go exits 0
    - grep -n "markerFor\|PRIVATE-FOR-SCHOOL" backend/internal/security/probes/probes_test.go exits 0
    - grep -n "t.Fatal" backend/internal/security/probes/probes_test.go exits 0 (fail-loud)
    - go build ./internal/security/probes/ in backend/ exits 0
    - go vet ./internal/security/probes/ in backend/ exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Kill-worker-mid-pipeline test</name>
  <files>backend/internal/queue/kill_worker_test.go</files>
  <action>
    Create backend/internal/queue/kill_worker_test.go that verifies the asynq doc-ingest pipeline is crash-safe: if the worker dies mid-pipeline, re-delivery/retry produces ZERO duplicate vectors and exactly ONE notification.

    Approach (mirror existing worker test patterns in backend/internal/queue/ if any — check worker_test.go existence first):
    1. Seed a school_1 tenant with a doc that has N chunks.
    2. Enqueue a TypeAIDocIngest task (check tasks.go for the exact constant name — if the phase-4 task is named differently, use the actual name).
    3. Use a hook to kill the worker after the insert_chunks call but before the notification send (e.g., a test-only context cancellation or a marker in the handler that simulates crash — check if the doc-ingest handler has a test seam; if not, simulate by calling the ingest handler with a context that cancels mid-way, or by splitting the pipeline steps and asserting the insert path is idempotent via ON CONFLICT DO NOTHING).
    4. Assert: after a simulated crash + retry, the vectors table has EXACTLY N rows for that document_id (zero duplicates — relies on insert_chunks ON CONFLICT (document_id, chunk_index) DO NOTHING), and the notification queue has EXACTLY 1 notification for that document (inspect the asynq task state or the notification table/queue).
    5. **Companion handler-idempotency fix (required — the current handler re-notifies on retry):** ground-checked that on a crash after Python-ingest succeeds but before recordSuccess→notify, an asynq retry re-runs the whole handler, re-ingests (idempotent via ON CONFLICT DO NOTHING — zero duplicate vectors guaranteed), re-sets status, and re-notifies → 2 notifications. Fix the doc-ingest handler (backend/internal/queue/doc_ingest_handler.go or wherever the handler lives — read_first to locate) so that when the document status is already "ready" (or the ingest already succeeded), the retry SKIPS re-ingest and SKIPS re-notification (dedupe). Add a ready-status guard at the top of the handler: if doc.Status == "ready", return success without re-ingesting or re-notifying. This makes the kill-worker test's "exactly one notification" assertion hold.
    6. Skip cleanly (t.Skip) when DB/Redis unavailable.

    If worker_test.go does not exist, check how backend/internal/queue/ is tested currently (maybe via scripts/test_endpoint.sh) and create the test file accordingly.
  </action>
  <read_first>
    backend/internal/queue/tasks.go (task constants, TaskPayload helpers)
    backend/internal/queue/worker.go (asynq Server, handlers)
    backend/internal/queue/doc_ingest_handler.go (locate the handler — add ready-status guard here)
    backend/internal/database/models/ai_document.go (doc status/notification fields)
    ai-engine/app/db/vectors.py (ON CONFLICT semantics — the Go path that calls Python insert)
  </read_first>
  <acceptance_criteria>
    - test -f backend/internal/queue/kill_worker_test.go
    - grep "func TestKillWorkerMidPipeline" backend/internal/queue/kill_worker_test.go exits 0
    - grep -n "DO NOTHING\|insert_chunks\|ON CONFLICT" backend/internal/queue/kill_worker_test.go exits 0 (idempotency assertion)
    - grep -n "notification\|Notify" backend/internal/queue/kill_worker_test.go exits 0 (exactly-one assertion)
    - grep -n '"ready"' backend/internal/queue/doc_ingest_handler.go exits 0 (ready-status guard added — dedupe re-ingest/notify)
    - go vet ./internal/queue/ in backend/ exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Extend k6 load test to 10x expected peak</name>
  <files>backend/.github/workflows/load-test.yml, backend/load-test/ (or wherever k6 scripts live)</files>
  <action>
    Extend the existing k6 load-test workflow to assert concurrency at 10× expected peak. Expected peak for AI endpoints per the rate-limit defaults from Phase 5 (D-03: per-school 300 req/min, per-user 60 req/min). 10× peak therefore means the load test should sustain 3000 req/min (50 req/s) across the AI endpoint mix.

    Read the existing load-test.yml and the k6 script(s) it invokes (check backend/load-test/ or backend/tests/ for .js/.ts k6 files). Extend:
    1. Add a k6 scenario "ai_10x_peak" that hits the AI endpoints (/api/v2/ai/chat, /api/v2/ai/providers, /api/v2/ai/search) at 50 iterations per second for a sustained 2-minute soak (VUs tuned to sustain 50 iter/s — e.g., 50 VUs × 1 iter/s, or whatever the script structure supports).
    2. Add assertions: p95 latency < 500ms for /ai/providers (cacheable) and no error rate > 1% for the suite. Keep threshold assertions aligned with what the existing workflow already asserts (read first — do not weaken existing thresholds).
    3. The workflow must still pass at lower intensity locally (smoke) — preserve existing test_type input handling (smoke default, full for 10×).

    Use k6 thresholds syntax in the script (thresholds: { "http_req_duration": ["p(95)<500"] }).
  </action>
  <read_first>
    backend/.github/workflows/load-test.yml (existing scenario + thresholds)
    backend/load-test/ or wherever the k6 scripts live (ls backend/load-test/ backend/tests/ first)
  </read_first>
  <acceptance_criteria>
    - grep -n "50" $(find backend -name "*.js" -o -name "*.ts" | xargs grep -l "k6\|http_req" 2>/dev/null | head -1) exits 0 (10x peak rate present)
    - grep -n "p(95)<500\|p(95) < 500" <the k6 script> exits 0
    - yaml validity: python3 -c "import yaml; yaml.safe_load(open('backend/.github/workflows/load-test.yml'))" exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Phase 5 review hardening — MA-01 cost-cap dead code</name>
  <files>backend/internal/middleware/ai_orchestrator.go, backend/internal/modules/ai/handler.go, backend/internal/modules/ai/stream_test.go, backend/internal/services/ai_orchestrator_service_test.go</files>
  <action>
    Fix MA-01: per-request cost cap (D-06) is dead code because CheckQuota is called with maxTokensEstimate=0.

    1. In backend/internal/middleware/ai_orchestrator.go (the AI middleware that calls aiOrchestratorService.CheckQuota), find the call site and the options/token budget available. The middleware receives the request; the handler knows opts.MaxTokens. Determine where the estimate should originate: read how the middleware is invoked in setup.go for the ai route group and how handlers pass options.
    2. The concrete fix: pass the token-budget estimate through. If the middleware has access to a token estimate from the request context (e.g., the handler sets it), use it; otherwise the middleware should accept a per-route max-token estimate via config (the AI scope already carries endpoint type — use the scope's max tokens) OR the handler should call a second CheckQuota with the real estimate before the provider call.
    3. Simplest correct approach (preferred): in handler.go for Chat and Search, before calling GenerateText, call aiOrchestratorService.CheckQuota(ctx, schoolID, opts.MaxTokens, provider) with the REAL opts.MaxTokens so the pre-call cost cap (D-06) actually trips. Keep the middleware's daily-cap CheckQuota (with 0 estimate — daily cap path unaffected) as the first-line guard. Update the test in services/ai_orchestrator_service_test.go (the existing test already covers per-request cap with 1_000_000 tokens) — add a handler-level test asserting a Chat request with MaxTokens above the cap returns ErrRequestCostExceeded/429.
    4. Verify CheckQuota signature: CheckQuota(ctx, schoolID int64, maxTokensEstimate int64, provider string) — pass opts.MaxTokens (int64) at the handler call.
  </action>
  <read_first>
    backend/internal/middleware/ai_orchestrator.go (CheckQuota call site)
    backend/internal/modules/ai/handler.go (Chat/Search handlers — where opts.MaxTokens is available)
    backend/internal/services/ai_orchestrator_service.go (CheckQuota signature + ErrRequestCostExceeded)
    backend/internal/modules/ai/stream_test.go (test patterns to extend)
  </read_first>
  <acceptance_criteria>
    - grep -n "CheckQuota" backend/internal/modules/ai/handler.go exits 0 (handler-level quota call with real estimate)
    - grep -n "opts.MaxTokens" backend/internal/modules/ai/handler.go exits 0
    - grep -rn "maxTokensEstimate" backend/internal/middleware/ai_orchestrator.go | head -2 (middleware path documented/kept)
    - go build ./... in backend/ exits 0
    - go test ./internal/modules/ai/ ./internal/services/ -count=1 exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Phase 5 review hardening — MA-02 ChatStream error classification</name>
  <files>backend/internal/ai/engine/python_provider.go, backend/internal/modules/ai/stream.go</files>
  <action>
    Fix MA-02: ChatStream does not classify non-200 responses into *StatusError, breaking D-14 error classification on the streaming path.

    1. In backend/internal/ai/engine/python_provider.go (or wherever ChatStream's HTTP response is handled — read the stream path in engine/client.go ChatStream and the pythonProvider's GenerateTextStream), ensure that when the Python engine returns a non-200 status, the response body is parsed into a *StatusError{StatusCode, Body} (the type already exists in engine.go).
    2. The streaming path must classify: 429 → mark breaker failure + failover to NEXT provider (never retry same); 5xx/timeout → retry once then failover; other 4xx → permanent, surface to caller as *StatusError (in-band error event per INT-01, not a broken stream).
    3. In backend/internal/modules/ai/stream.go, verify the SSE relay maps *StatusError to an in-band "error" event with status code (per INT-01 contract) and does not break the stream. If the relay currently emits a generic error, update it to include the status code.
    4. Add/extend a test (stream_test.go or python_provider_test.go) that simulates a non-200 from the Python engine and asserts the *StatusError classification + failover decision.
  </action>
  <read_first>
    backend/internal/ai/engine/engine.go (StatusError type)
    backend/internal/ai/engine/client.go (ChatStream HTTP handling)
    backend/internal/ai/engine/python_provider.go (GenerateTextStream)
    backend/internal/modules/ai/stream.go (SSE relay error mapping)
    backend/internal/modules/ai/stream_test.go
  </read_first>
  <acceptance_criteria>
    - grep -n "StatusError" backend/internal/ai/engine/python_provider.go exits 0 (classification present)
    - grep -n "429\|StatusUnauthorized\|5xx\|status code\|StatusCode" backend/internal/modules/ai/stream.go exits 0 (in-band error event carries status)
    - go build ./... in backend/ exits 0
    - go test ./internal/ai/engine/ ./internal/modules/ai/ -count=1 exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Phase 5 review hardening — MA-03 pythonProvider param passthrough</name>
  <files>backend/internal/ai/engine/python_provider.go, backend/internal/ai/engine/engine.go, ai-engine/app/api/chat.py, backend/internal/ai/engine/python_provider_test.go</files>
  <action>
    Fix MA-03: pythonProvider silently drops Temperature/MaxTokens when adapting the ai.Provider interface to the EngineClient ChatRequest.

    1. In backend/internal/ai/engine/python_provider.go, find GenerateText/GenerateTextStream where the ai.Options (Temperature, MaxTokens) are translated into the EngineClient ChatRequest.
    2. Ensure the adapter copies ai.Options.Temperature and ai.Options.MaxTokens into the ChatRequest fields (check the ChatRequest struct in engine.go for existing Temperature/MaxTokens fields — if the struct lacks them, add them as *float64/*int64 or the type the Python API expects; read engine.go and the Python chat.py request model to match field names/types).
    3. Verify Python side (ai-engine/app/api/chat.py) already accepts temperature/max_tokens in its request model — if not, add them (and a test). The translation must be explicit: defaults preserved when zero values (use pointers or omitempty semantics).
    4. Add/extend a test (embed_spike_test.go or a new python_provider_test.go) asserting Temperature/MaxTokens are passed through to the ChatRequest.
  </action>
  <read_first>
    backend/internal/ai/engine/engine.go (ChatRequest struct fields)
    backend/internal/ai/engine/python_provider.go (adapter translation)
    ai-engine/app/api/chat.py (request model — temperature/max_tokens fields)
    backend/internal/ai/gateway.go (ai.Options fields Temperature/MaxTokens)
  </read_first>
  <acceptance_criteria>
    - grep -n "Temperature\|MaxTokens" backend/internal/ai/engine/python_provider.go exits 0 (passthrough present)
    - grep -n "temperature\|max_tokens" ai-engine/app/api/chat.py exits 0 (Python accepts params)
    - go build ./... in backend/ exits 0
    - go test ./internal/ai/engine/ -count=1 exits 0
    - python -m pytest ai-engine/tests/test_chat.py -q --tb=short in ai-engine/ exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Wire probes + kill-worker test into CI (B1)</name>
  <files>backend/.github/workflows/ci.yml, backend/.github/workflows/rag-eval.yml</files>
  <action>
    Wire the cross-tenant probes and kill-worker test into CI so TES-01's "in CI on every deploy" is actually delivered.

    1. backend/.github/workflows/ci.yml: the `integration-tests` job currently runs only `./internal/database/... ./internal/service/...` (line ~172 — read_first to confirm). Extend the go test path list to also cover `./internal/security/... ./internal/queue/...`, and pass the integration build tag (e.g., `-tags integration`) so the `//go:build integration` probe package runs. Also set the DB env vars the probes need (the same AI_PGVECTOR_DSN / test DB DSN the job already sets for database tests — match the existing env var names).
    2. backend/.github/workflows/rag-eval.yml (created in 06-03): add a second job (or extend the existing eval job) that runs the Python cross-tenant probe test file: `python -m pytest ai-engine/tests/test_cross_tenant.py -q --tb=short` with AI_PGVECTOR_DSN set. This runs on deploy (same triggers as the RAG eval workflow, plus pull_request to backend/**).

    Match existing CI conventions exactly (env var names, service containers, checkout with submodules recursive).
  </action>
  <read_first>
    backend/.github/workflows/ci.yml (integration-tests job — go test invocation, tags, env vars)
    backend/.github/workflows/rag-eval.yml (06-03 Task 3 output — where to add the pytest job)
  </read_first>
  <acceptance_criteria>
    - grep -n "internal/security" backend/.github/workflows/ci.yml exits 0 (probes in CI path list)
    - grep -n "internal/queue" backend/.github/workflows/ci.yml exits 0 (kill-worker test in CI path list)
    - grep -n "integration" backend/.github/workflows/ci.yml exits 0 (build tag passed)
    - grep -n "test_cross_tenant" $(ls backend/.github/workflows/rag-eval.yml .github/workflows/rag-eval.yml 2>/dev/null | head -1) exits 0 (Python probes in CI)
    - python3 -c "import yaml; yaml.safe_load(open('backend/.github/workflows/ci.yml'))" exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 8: Python-side adversarial cross-tenant probes (B2)</name>
  <files>ai-engine/tests/test_cross_tenant.py</files>
  <action>
    Create ai-engine/tests/test_cross_tenant.py — Python-side adversarial cross-tenant probe suite (D-08 explicitly requires "Python search/extract" entry points be probed).

    Follow the DB-gated pattern from ai-engine/tests/test_search.py exactly:
    - LIVE_DB = pytest.mark.skipif(not os.getenv("AI_PGVECTOR_DSN"), reason="no AI_PGVECTOR_DSN (DB-gated tests skip cleanly)")
    - Seed two tenant schemas school_1 and school_2 (mirror how test_search.py seeds school_1 — extend to seed school_2).
    - Insert marker chunks: school_1 gets "PRIVATE-FOR-SCHOOL-ONE-<uuid>" content, school_2 gets "PRIVATE-FOR-SCHOOL-TWO-<uuid>" content (same vector dimension VECTOR_1536 = [0.1]*1536 pattern, embedding stubbed as test_search.py does).
    - Adversarial queries:
      - test_cross_tenant_search: search as school_1 → assert NO "PRIVATE-FOR-SCHOOL-TWO" marker in results; search as school_2 → assert NO "PRIVATE-FOR-SCHOOL-ONE" marker.
      - test_cross_tenant_extract_documents: GET/POST documents list as school_1 → assert school_2's document ids/markers absent.
      - test_schema_header_required: no X-School-Schema header → 400 (no fallback — D-06).
    - Each assertion uses pytest.fail (or assert) on any leak.

    Use the ASGITransport/AsyncClient pattern from test_search.py.
  </action>
  <read_first>
    ai-engine/tests/test_search.py (DB-gated pattern, seeding, ASGITransport, VECTOR_1536)
    ai-engine/app/api/search.py (search response shape)
    ai-engine/app/api/extract.py (documents endpoint + schema header)
  </read_first>
  <acceptance_criteria>
    - test -f ai-engine/tests/test_cross_tenant.py
    - grep "def test_cross_tenant_search" ai-engine/tests/test_cross_tenant.py exits 0
    - grep "PRIVATE-FOR-SCHOOL-ONE\|PRIVATE-FOR-SCHOOL-TWO" ai-engine/tests/test_cross_tenant.py exits 0
    - grep "LIVE_DB\|AI_PGVECTOR_DSN" ai-engine/tests/test_cross_tenant.py exits 0 (DB-gated)
    - python -m pytest ai-engine/tests/test_cross_tenant.py -q --tb=short in ai-engine/ exits 0 (or skips cleanly without DSN)
  </acceptance_criteria>
</task>

</tasks>

<verification_criteria>
- go build ./... in backend/ exits 0
- go test ./internal/security/probes/ ./internal/queue/ ./internal/ai/engine/ ./internal/modules/ai/ ./internal/services/ -count=1 exits 0
- python -m pytest ai-engine/tests/test_chat.py -q --tb=short in ai-engine/ exits 0
- grep "CheckQuota" backend/internal/modules/ai/handler.go exits 0 (MA-01 fixed)
- grep "StatusError" backend/internal/ai/engine/python_provider.go exits 0 (MA-02 fixed)
- grep "Temperature" backend/internal/ai/engine/python_provider.go exits 0 (MA-03 fixed)
- Cross-tenant probes + kill-worker test present and vet-clean
</verification_criteria>
